@model List<WorkWithFarmacy.Models.Preorder>

@{
    ViewData["Title"] = "Preorder";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Preorder</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Count )
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Count)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nnt)
            </td>            
            <td>
                @Html.DisplayFor(modelItem => item.SupInn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SupDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrclDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrcOptNds)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PlType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
